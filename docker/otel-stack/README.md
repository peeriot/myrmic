
# Grafana OpenTelemetry Stack

This `docker-compose` setup deploys a Grafana stack for testing OpenTelemetry. Grafana will be reachable under: http://localhost:3000

```bash
docker compose up -v
docker compose down # add -v to delete volumes to start from scratch
```
