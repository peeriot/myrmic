[package]
name = "module-examples-common"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
postcard = { version = "1.1.3", default-features = false, features = ["alloc"] }
serde = { version = "1.0.216", default-features = false, features = [
    "derive",
    "alloc",
] }
